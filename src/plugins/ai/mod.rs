// AI plugin — Ollama-backed LLM summaries and community insights.
//
// The implementation lives in crate::intelligence (the original module).
// This module exists to declare the plugin boundary and will absorb
// that code in a future migration step.
//
// Feature: "ai"

// The implementation lives in crate::intelligence.
// Access it via crate::intelligence directly for now.

